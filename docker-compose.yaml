services:
  turtlebot-core:
    image: nobel86/turtlebot-rpi5:zenoh  # Zenoh test image - keep :latest untouched until validated
    network_mode: host
    stdin_open: true
    tty: true  
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
      - XAUTHORITY=${XAUTH}
      - RMW_IMPLEMENTATION=rmw_zenoh_cpp
      # Fallback middleware, kept for reference / easy rollback:
      #   RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
      - CYCLONEDDS_URI=file:///root/turtlebot3_ws/cylconedds_config.xml
      # Zenoh router (started via zenoh_start_router.sh in the image) listens
      # on tcp/0.0.0.0:7447. network_mode: host already exposes it on the
      # robot's IP (e.g. its AP address 10.42.0.1) - no extra ports: needed.
      - LDS_MODEL=${LDS_MODEL}
      - TURTLEBOT3_MODEL=burger
      - TURTLEBOT3_NAME=TB3_${TURTLEBOT_NR}
      - ROS_DOMAIN_ID=${ROS_DOMAIN_ID}

    container_name: turtlebot_${TURTLEBOT_NR}
    privileged: true
    cap_add:
      - NET_ADMIN
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    command: bash 
    devices:
      - /dev/ttyACM0:/dev/ttyACM0  # OpenCR
      - /dev/ttyUSB0:/dev/ttyUSB0  # LDS 01 - laser lidar
      - /dev/input/js0:/dev/input/js0  # Logitech F710 Gamepad
      - /dev/video0:/dev/video0  # Camera
